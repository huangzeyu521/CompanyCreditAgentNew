# 《企业信用状况综合评价智能体》前端开发执行方案（颗粒到组件）

> 严格按本方案逐步骤完成 → 每步完成后立即自检 → 不通过即原地修复直至达标。

## 步骤 0：项目初始化（验收：目录结构齐全）
- 创建目录：`frontend/{pages,assets/{css,js,img},data/sfecr-extracts,docs,tools}`
- 添加 README.md（启动说明、合规说明、目录解释）
- 自检：`ls frontend/` 返回所有子目录

## 步骤 1：sfecr 文案抽取（验收：8 个核心 .md 落地）
- 用 `pdftotext` 抽取以下来源（从 materials/sfecr 已有文件）：
  - 评级业务程序指引（业务制度/1719568169829.pdf）
  - 信用评级业务尽职调查制度（业务制度/1719568222345.pdf）
  - 跟踪评级制度（业务制度/1719567489773.pdf）
  - 评级结果验证和公布制度（业务制度/1715235051594.pdf）
  - 信用等级划分及定义（业务制度/1719567922181.pdf）
  - 信用评级方法规则（业务制度/1719568006939.pdf）
  - 信用评审委员会制度（业务制度/1719568690822.pdf）
  - 主体评级方法学样例（保险公司方法学）作八大维度示例
- 输出 `data/sfecr-extracts/*.md`，每个 md 头部标注来源 PDF。
- 自检：每份 .md 至少 30 行，且 frontmatter 包含 `source: ...`

## 步骤 2：Tailwind 配置 + 自定义 theme.css（验收：12 个 utility 类生效）
- `assets/js/tailwind.config.js`：注入 brand/accent/success/warning/danger/gold/ink/muted/bg/border 色板与 shadow。
- `assets/css/theme.css`：实现 12 个自定义类（shadow-card/lift/breathe/pulse-dot/fade-in-up/grad-text/grad-deep-blue/glow-gold/th/toc-active/toc-item/step-card/step-bar/step-indicator/no-print/num）。
- 自检：在 workbench 里临时挂一个 `breathe`、`grad-text`、`pulse-dot` 验证生效。

## 步骤 3：mock-data.js（验收：14 个数据键齐全且互相一致）
- 完整提供原型契约的所有 MockData 字段，至少 5 个 subjects、12 reports、10 audit logs、12 系统用户、20 数据源、8 attribution、6 translations、8 industries、6 alerts、6 todos、12 apis、5 tracking subscriptions。
- 自检：在 console 检查 `MockData` 全部键存在。

## 步骤 4：common.js（通用组件函数）
- gradeBadge / fusionBadge / getGradeBgClass / getGradeColorClass / getGradeSemantic
- toast / openModal / closeModal / confirmDialog
- fmtNum / fmtDate
- 自检：浏览器 console 调 `toast('hello','success')` 正常显示。

## 步骤 5：layout.js（顶栏 + 侧栏 + AI 抽屉）
- mountLayout(active, renderMain)
- 顶 logo、面包屑、通知、AI 助手入口（🤖）、用户头像
- 侧 11 项菜单
- AI 助手抽屉容器
- 自检：在 workbench 等任意页"AI"按钮点击能弹出抽屉。

## 步骤 6：copy-corpus.js（合规文案库）
- 导出 `window.Copy`（多个模块：BUSINESS、COMPLIANCE、REPORT、GRADES、TRACKING、KYC、INDUSTRY、AUDIT、SYSTEM、UI）
- 每个 value 形如 `{text:'...', source:'sfecr/业务制度/xxx.pdf'}`。
- 自检：`Object.keys(Copy).length >= 8`，`Copy.GRADES.AAA.text === '偿还债务的能力极强...'`。

## 步骤 7：prompts.js（12 个 System Prompt）
- 每个 Prompt = 全局首部 + 任务专属。
- 自检：导出 `Prompts.RATING_DRAFT、Prompts.PUB_CREDIT_ATTRIB、…` 全部存在。

## 步骤 8：ai-agent.js（DashScope 流式调用 + 思考过程引擎）
- `AIAgent.invoke({scene, userInput, context, onPhase, onDelta, onDone, onError})`
- 6 步骤可视化（兜底节流 + STEP 分隔解析）
- 自检：mock 模式下点击 AI 助手"开始"，能看到 6 个步骤逐步前进 + 最终输出。

## 步骤 9：login.html（独立布局）
- 左渐变深蓝 + 4 步功能列表 + 4 合规徽章
- 右账号密码 / SSO 切换 + 7天免登录 + 主按钮跳 workbench
- 自检：填 admin / 任意密码点登录跳转 workbench。

## 步骤 10：workbench.html
- 复刻原型；KPI/五步/待办/报告/预警/行业洞察/隐私计算盒
- 文案 100% 取自 Copy
- 自检：与原型并排截图视觉一致。

## 步骤 11：subject-rating.html（核心五步 + AI 助手集成）
- 步骤指示器、5 步 render、各步 ECharts、AI 助手按钮
- 步 5 命中 D 级时的 grade 限制
- 自检：选择不同主体 + 切换步骤 + AI 助手抽屉调用流畅。

## 步骤 12：bond-rating.html
- 主体快照 + 表单 + 增信 + 三等级
- 自检：visual diff 通过；点"生成"跳报告详情。

## 步骤 13：kyc.html
- 输入 → 红黄绿判定 → 4KPI + 详情
- 自检：D 级主体显示红色"不建议承接"。

## 步骤 14：industry-scan.html
- KPI×5、热力柱状、12月趋势、分布柱、TOP5、熔断专列、ECharts × 3
- 自检：图表正确渲染、label 不溢出。

## 步骤 15：tracking.html
- 4KPI + 7日趋势(stack bar) + 饼图 + 订阅卡 + 实时预警 + 添加订阅 modal
- 自检：添加订阅弹窗、表单与按钮事件正常。

## 步骤 16：monitoring-report.html
- 周期、6KPI、双趋势图、报送底稿、历史记录、详情 modal
- 自检：历史详情打开正常；图表渲染。

## 步骤 17：reports.html
- 筛选 + 表格 + 分页 + CSV 导出
- 自检：search/type/status 联动正确；CSV 内容含中文 BOM。

## 步骤 18：report-detail.html（14 节）
- 顶 Header + 元数据条 + 左 TOC 14 项 + 14 sections
- 雷达 + 力图 + scrollspy
- 文案 100% 来自 Copy / sfecr-extracts
- 自检：滚到 sec-13 双轨融合声明显示金色卡 + 4 徽章。

## 步骤 19：audit-log.html
- 5KPI + 24h 柱状 + TOP5 饼图 + 筛选 + 表格 + 区块链验证 modal + 详情 modal
- 自检：点详情显示 JSON；区块链验证显示 Merkle 根。

## 步骤 20：openapi.html
- 4KPI + 14 天双轴趋势 + TOP5 + API 文档（按 group 折叠） + Token + 限流 + 计费
- 自检：API 折叠展开正常；图表正确。

## 步骤 21：system.html（7 Tab）
- 用户、角色、租户、数据源、模型、接口、监控（含 SLA 柱状 + 24h 时延趋势 + 服务列表）
- 自检：Tab 切换重渲染图表；用户编辑/禁用 modal 正常。

## 步骤 22：404.html
- 大字渐变 + 返回工作台
- 自检：渐变文字显示。

## 步骤 23：合规与一致性
- README、文案 lint 自检脚本（grep 关键词与 Copy 库交叉）

## 步骤 24：本地启动测试
- `python -m http.server 8000` from `frontend/`
- 浏览访问 `/pages/login.html`
- 自检：14 页全部可达，console 无报错。

## 步骤 25：全量测试与修复（详见 05 测试报告）
- 功能 / 还原 / 大模型 / 文案 / 兼容 / 响应式 / 性能
